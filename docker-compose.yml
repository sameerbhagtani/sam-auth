services:
    postgres:
        image: postgres:17
        container_name: sam-auth-postgres
        restart: unless-stopped

        ports:
            - "5432:5432"

        environment:
            POSTGRES_USER: user
            POSTGRES_PASSWORD: password
            POSTGRES_DB: sam_auth_db

        volumes:
            - postgres_data:/var/lib/postgresql/data

volumes:
    postgres_data:
        name: sam-auth-postgres-data
